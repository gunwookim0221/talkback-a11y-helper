<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".A11yHelperService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/a11y_service_config" />
        </service>

        <receiver
            android:name=".A11yCommandReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.a11yhelper.GET_FOCUS" />
                <action android:name="com.example.a11yhelper.DUMP_TREE" />
                <action android:name="com.example.a11yhelper.FOCUS_TARGET" />
                <action android:name="com.example.a11yhelper.CLICK_TARGET" />
                <action android:name="com.example.a11yhelper.NEXT" />
                <action android:name="com.example.a11yhelper.PREV" />
                <action android:name="com.example.a11yhelper.CLICK_FOCUSED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
